{
    "enabled": {
        "description": "是否启用反垃圾/反刷屏功能",
        "type": "bool",
        "default": true
    },
    "duplicate_threshold": {
        "description": "重复消息检测阈值：在时间窗口内发送相同消息达到此次数触发冷却",
        "type": "int",
        "default": 3,
        "hint": "最小值 2"
    },
    "duplicate_window_seconds": {
        "description": "重复消息检测时间窗口（秒）",
        "type": "int",
        "default": 10,
        "hint": "最小值 5"
    },
    "flood_threshold": {
        "description": "刷屏检测阈值：在时间窗口内发送消息总数达到此次数触发冷却",
        "type": "int",
        "default": 5,
        "hint": "最小值 3"
    },
    "flood_window_seconds": {
        "description": "刷屏检测时间窗口（秒）",
        "type": "int",
        "default": 10,
        "hint": "最小值 5"
    },
    "cooldown_seconds": {
        "description": "触发刷屏检测后的冷却时间（秒），冷却期间消息被静默丢弃",
        "type": "int",
        "default": 30,
        "hint": "最小值 10"
    }
}
